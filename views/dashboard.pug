extends layout

block content
    h2 Toggle Dark/Light mode 
        p Click the button to toggle between dark and light mode.
            button(onclick="toggleTheme('dark')") Dark
            button(onclick="toggleTheme('light')") Light
    //- script (type="text/javascript").
    //-     function myFunction() {
    //-      var element = document.body;
    //-         element.classList.toggle("dark-mode"); }